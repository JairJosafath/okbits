import React from "react";

export const metadata = {
  title: "Log In | OkBits",
  description: "Log In to OkBits",
};

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
